<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">
    <data class="CertificatviewModelBinding">

        <import type="android.view.View" />

        <variable
            name="model"
            type="fudex.bonyad.viewmodel.technical.CertificatViewModel" />
    </data>
    <FrameLayout
        android:layout_width="70dp"
        android:layout_height="60dp"
        android:layout_marginEnd="15dp"
        android:background="@drawable/rec_strockdarkblue">
        <FrameLayout
            android:visibility="@{model.onservse.id == -1 ? View.GONE : View.VISIBLE}"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="4dp">
            <ImageView
                android:layout_width="50dp"
                android:layout_height="50dp"
                bind:certificate="@{model.onservse}"
                android:layout_gravity="center"/>
            <ImageView
                android:onClick="@{ () -> model.delete() }"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:background="@drawable/ic_deleteimage"/>
        </FrameLayout>
    <FrameLayout
        android:visibility="@{model.onservse.id == -1 ? View.VISIBLE : View.GONE}"
        android:layout_width="match_parent"
        android:onClick="@{ () -> model.clickitem() }"
        android:layout_height="match_parent"
        android:layout_margin="4dp">
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_gravity="center">
            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/group_7"
                android:layout_gravity="center"/>
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@string/attach_a_picture"
                android:textSize="8sp"
                android:layout_marginTop="8dp"
                android:textColor="@color/darkblue"
                android:fontFamily="@font/cairoregular"/>
        </LinearLayout>
    </FrameLayout>
    </FrameLayout>
</layout>